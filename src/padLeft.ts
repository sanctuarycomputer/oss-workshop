/**
 * Pad a string to a certain length by prepending additional characters.
 * If the string is already longer than the `places` argument, it is returned
 * as-is.
 *
 * I.e.:
 *
 * padLeft('A', 'x', 3) => 'xxA'
 * padLeft('ABC', 'x', 3) => 'ABC'
 * padLeft('ABCD', 'x', 3) => 'ABCD'
 */
export const padLeft = (
  text: string,
  fillValue: string,
  places: number
): string => {
  return 'oops'
}
